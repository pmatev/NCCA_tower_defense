#ifndef FWD_UIBUTTON_H
#define FWD_UIBUTTON_H

#include "smartpointers.h"

DECLARESMART(UIButton)

enum LabelPosition {LEFT, RIGHT, TOP, BOTTOM};

enum ButtonState {DEFAULT, PRESSED, HOVER, DISABLE, CREATEON};

#endif // UIBUTTON_H
